<?php
    define('DEBUG',true);
    define('DEFAULT_LAYOUT','default'); //if there is no layout controller use this layout
    define('DEFAULT_CONTROLLER','Home'); //default controller if there isn't one defined in the url
    define('SITE_TITLE','E-Commerce-PlatForm'); //if there is no site title this will be the the site title
    define('PROOT','/Single-Vendor-E-Commerce-Platform/'); //set this to '/' for a live server